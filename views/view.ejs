<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rvAZxk/RPpwSXP0EC2sA0TOlRjPKCfQC5N9+1wQ8efdB8xOISwXpQVoz/+GhJGJo"
        crossorigin="anonymous">
    <link rel="stylesheet" href="https://kit.fontawesome.com/a5f47a3b96.js" crossorigin="anonymous">
    <title>Your To-Do List</title>
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
        }

        .container {
            padding: 20px;
            display: flex;
            gap: 20px;
            max-width: 800px;
            margin: auto;
        }

        .left-side {
            flex: 1;
        }

        .right-side {
            flex: 1;
        }

        h1,
        h2 {
            font-size: 24px;
            color: #333;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            margin-bottom: 6px;
            color: #555;
        }

        input[type="text"],
        select,
        input[type="date"] {
            width: 100%;
            padding: 8px;
            border: 1px solid #ced4da;
            border-radius: 4px;
            font-size: 16px;
        }

        button[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
        }

        .list {
            margin-top: 30px;
            text-align: left;
        }

        li {
            background-color: #fff;
            border: 1px solid #ced4da;
            border-radius: 4px;
            padding: 15px;
            margin-bottom: 15px;
            transition: border-color 0.3s;
        }

        li:hover {
            border-color: #007bff;
        }

        li.work {
            border-color: #dc3545;
        }

        li.personal {
            border-color: #28a745;
        }

        li.shopping {
            border-color: #ffc107;
        }

        .delete-button {
            text-align: right;
            margin-top: 10px;
        }

        .delete-button a {
            text-decoration: none;
            border: 1px solid #ced4da;
            border-radius: 4px;
            padding: 5px 10px;
            font-size: 14px;
            color: #333;
            transition: background-color 0.3s;
        }

        .delete-button a:hover {
            background-color: #dc3545;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="left-side">
            <h1 class="mb-4">To-Do List</h1>
            <form id="todo-form" method="post" action="/form_data">
                <div class="form-group">
                    <label for="taskDescription">Task Description:</label>
                    <input type="text" id="taskDescription" name="taskDescription" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="category">Category:</label>
                    <select id="category" name="category" class="form-control" required>
                        <option value="work" class="work">Work</option>
                        <option value="personal" class="personal">Personal</option>
                        <option value="shopping" class="shopping">Shopping</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="taskDate">Date:</label>
                    <input type="date" id="taskDate" name="taskDate" class="form-control" required>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Add Task</button>
                </div>
            </form>
        </div>
        <div class="right-side">
            <h2 class="mb-4">Responses</h2>
            <ul class="list-unstyled">
                <% for(let i of data_list){ %>
                <li class="mb-3 <%= i.category %>">
                    <p>
                        <i class="fa-solid fa-thumbtack"></i> Task Description: <%= i.taskDescription %><br>
                        Category: <%= i.category %><br>
                        Date: <i class="fa-solid fa-calendar-days"></i> <%= i.taskDate %><br>
                    </p>
                    <div class="delete-button">
                        <a href="/delete-contact/?id=<%= i.id %>" class="btn btn-danger">
                            <i class="fa-solid fa-check"></i> Completed
                        </a>
                    </div>
                </li>
                <% } %>
            </ul>
        </div>
    </div>

    <!-- Bootstrap JS dependencies (Popper.js, Bootstrap JS) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
        integrity="sha384-LtS9Jhbm1J46R27G9DS89E3Qss/T9zS1/0SLwFZGj8Csqd+LZO9KwJdAMC5l9ww"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"
        integrity="sha384-XaCvrT9sR0D58DweR8GLf/HTISl/jYXPov8fUa6x6tka0N4lNM3g4vBcg4vL1pi6"
        crossorigin="anonymous"></script>
</body>

</html>
